<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation">
    <activity
        android:id="@+id/calendarActivity"
        android:name="com.example.zephyrus.CalendarActivity"
        android:label="activity_calendar"
        tools:layout="@layout/activity_calendar" />
    <activity
        android:id="@+id/spreadsActivity"
        android:name="com.example.zephyrus.SpreadsActivity"
        android:label="activity_spreads"
        tools:layout="@layout/activity_spreads" />
    <activity
        android:id="@+id/cardListActivity"
        android:name="com.example.zephyrus.CardListActivity"
        android:label="activity_card_list"
        tools:layout="@layout/activity_card_list" />
    <activity
        android:id="@+id/journalActivity"
        android:name="com.example.zephyrus.JournalActivity"
        android:label="activity_journal"
        tools:layout="@layout/activity_journal" />
    <activity
        android:id="@+id/settingsActivity"
        android:name="com.example.zephyrus.SettingsActivity"
        android:label="activity_settings"
        tools:layout="@layout/activity_settings" />
</navigation>